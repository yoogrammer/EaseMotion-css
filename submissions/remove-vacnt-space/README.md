# UI Fixes & Enhancements

The `remove-vacant-space` folder is dedicated to removing the vacant space below the navbar in the mobile viewport.

It contains corrected layout logic that changes how the header and document shell interact on smaller screens, effectively closing the gap without breaking the sidebar toggle functionality.