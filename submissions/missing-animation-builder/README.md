# Fix: Sidebar Navigation Missing Items
- **Added:** The "Animation Builder" link to the HTML structure within the "Core" sidebar group (`<ul class="sidebar-nav">`).
- **Note:** Styling inherits naturally from the existing `docs.css` file without additional modifications.