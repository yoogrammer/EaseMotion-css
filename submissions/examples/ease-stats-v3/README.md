# Dashboard Metrics Grid

A responsive statistics panel card component containing flexible layout engines, inline SVG assets, and clean interaction states.

## Core Features
- Responsive design adapting smoothly across Desktop (4 columns), Tablet (2 columns), and Mobile (1 column).
- Micro-interactions handling smooth spring translation vectors on user card hover.
- Pure inline SVG paths to remove image asset dependancies and styling lag.