# ease-notification-bell-ring

A bell icon notification component with a ring animation.

## Features

- SVG bell icon with badge dot
- Tilt rotation keyframes for realistic ringing
- Smooth animation controlled via CSS custom property
- Dark theme styling

## Usage

Set `--bell-ring: 1` on the `.nbr-bell` element to trigger the ring animation. Set it back to `0` to reset.
