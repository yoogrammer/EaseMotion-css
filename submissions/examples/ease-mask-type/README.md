# ease-mask-type

CSS utility classes for the `mask-type` property.

## Usage

```html
<div class="ease-mask-type-example">Content</div>
```

## Classes

See `style.css` for the full list of available classes.

## Demo

Open `demo.html` to see the utility classes in action.
