# EaseTooltip Component (`ease-elastic-tooltip`)

A zero-dependency, pure CSS micro-interaction component for displaying tooltips with smooth, elastic expansion tracking.

## Usage

Simply include the stylesheet and append a `data-tooltip` attribute containing your desired string to any HTML element:

```html
<button data-tooltip="Your tooltip text goes here!">Hover Me</button>