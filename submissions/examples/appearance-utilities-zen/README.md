# Appearance Utilities

CSS utility class for resetting native element appearance.

## Classes

- `.appearance-none` — `appearance: none`
- `.appearance-auto` — `appearance: auto`

## Why

Removes default browser styling from form elements like `<select>`, `<input>`, and `<button>` for custom styling.
