# Magnetic Button Variant (`magnetic-button-pn`)

A magnetic hover micro-interaction component created for EaseMotion CSS.

## Features
- Smooth mouse-tracking movement vectors utilizing JavaScript `getBoundingClientRect`.
- Dynamic customization configuration via `data-strength`.
- Automated accessibility fail-safe matching system `prefers-reduced-motion`.