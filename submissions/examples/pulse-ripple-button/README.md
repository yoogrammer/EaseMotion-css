# Pulse Ripple Button

A simple standalone demo showing a button with a ripple-on-click animation and optional pulse effect.

How to use
- Open `demo.html` in a browser (double-click the file).
- Click the "Click Me" button to see the ripple animation.
- To enable continuous pulse, uncomment the CSS block in `style.css`.
