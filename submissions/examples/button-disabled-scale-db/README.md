# Disabled Button Scale Block

1. What does this do? Stops click shrink effects from firing on disabled interactive buttons.
2. How is it used? References active selectors using negative CSS state attributes.
3. Why is it useful? Prevents confusion by matching visual layout feedback with action states.
