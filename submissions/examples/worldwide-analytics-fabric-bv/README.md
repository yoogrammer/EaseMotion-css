# Worldwide Analytics Fabric

A premium enterprise analytics dashboard showcasing edge performance logs, data pipeline stats, and incident lists using pure CSS.

## Usage
Open `demo.html` directly in a browser — no server needed.

## Why EaseMotion CSS?
Demonstrates premium CSS layout, pulse animations, hover transitions, and slide-in log animations with zero JS dependencies.