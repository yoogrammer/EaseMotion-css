# CSS text-box-trim Example (#9146)

## Description
This example demonstrates the modern `text-box-trim` and `text-box-edge` properties to remove font-specific vertical leading, allowing for precise layout control in headings and UI components.

## Why EaseMotion CSS?
As a motion-first framework, precise text alignment is crucial for smooth layout transitions and avoiding 'layout shift' during animations.