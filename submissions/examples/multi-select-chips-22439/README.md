# Multi-select Chips
A selection component using CSS-only checkbox state management.

- Uses `input[type="checkbox"]` with the sibling selector `+` to toggle styles.
- Fully accessible via `label` wrappers.
- Highly performant with no JavaScript dependencies for interactivity.