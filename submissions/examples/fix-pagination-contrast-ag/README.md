# Fix ease-pagination Contrast

## What does this do?
Improves the contrast ratio of the `pagination` component in dark mode.

## How is it used?
Include the component as usual. The new CSS variables and dark mode media queries will handle the contrast automatically.

## Why is it useful?
Ensures compliance with WCAG AA accessibility standards.
Fixes: #35955
