# Global Operations Control Tower

A premium enterprise dashboard with operations charts, logistics flow tracking, and regional status grids using pure CSS.

## Usage
Open `demo.html` directly in a browser — no server needed.

## Why EaseMotion CSS?
Demonstrates premium CSS layout, pulse animations, hover transitions, and fade-up effects with zero JS dependencies.