# Mask Composite — EaseMotion CSS Utilities

CSS `mask-composite` utility classes for the EaseMotion CSS framework.

## Quick Start

```html
<link rel="stylesheet" href="style.css">
```

## Utility Classes

| Class | Declaration |
|-------|-------------|
| `.mask-add` | `mask-composite: add;` |
| `.mask-subtract` | `mask-composite: subtract;` |
| `.mask-intersect` | `mask-composite: intersect;` |
| `.mask-exclude` | `mask-composite: exclude;` |
| `.mask-copy` | `mask-composite: copy;` |
| `.mask-source-out` | `mask-composite: source-out;` |
| `.mask-source-in` | `mask-composite: source-in;` |
| `.mask-source-atop` | `mask-composite: source-atop;` |
| `.mask-destination-over` | `mask-composite: destination-over;` |
| `.mask-destination-in` | `mask-composite: destination-in;` |
| `.mask-destination-out` | `mask-composite: destination-out;` |
| `.mask-destination-atop` | `mask-composite: destination-atop;` |
| `.mask-clear` | `mask-composite: clear;` |
| `.mask-xor` | `mask-composite: xor;` |
| `.mask-plus` | `mask-composite: plus;` |
| `.mask-plus-lighter` | `mask-composite: plus-lighter;` |
| `.mask-plus-darker` | `mask-composite: plus-darker;` |
| `.mask-inherit` | `mask-composite: inherit;` |
| `.mask-initial` | `mask-composite: initial;` |

## Responsive Variants

Prefix with `sm-`, `md-`, `lg-` for responsive behavior:

```html
<div class="util-class sm-util-class md-util-class lg-util-class">
  Responsive Mask Composite
</div>
```

## Dark Mode

Use the `-dark` variant:

```html
<div class="util-class-dark">
  Dark mode Mask Composite
</div>
```

## Reduced Motion

Use the `-nomotion` variant:

```html
<div class="util-class-nomotion">
  No motion Mask Composite
</div>
```

## Framework Integration

All utilities use EasMotion design tokens from `core/variables.css`: `--ease-primary`, `--ease-secondary`, `--ease-accent`, `--ease-success`, `--ease-danger`, `--ease-warning`, `--ease-info`.

## License

MIT License — © 2026 EaseMotion Contributors
