# Masonry Column Breaks Fix

1. What does this do? Prevents split-column rendering glitches inside masonry child items.
2. How is it used? Set column breaking constraints directly on nested elements.
3. Why is it useful? Guarantees rendering consistency across Firefox and Webkit layouts.
