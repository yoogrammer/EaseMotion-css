# Zoom Out Animation SG

## What does this do?
This submission provides a self-contained zoom-out animation for highlighting UI cards, modals, and feedback elements.

## How is it used?
Add the `zoom-out` class to the element that should animate.

```html
<div class="zoom-card zoom-out">
  Zoom Out
</div>