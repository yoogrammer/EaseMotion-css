# CSS-Only Animated Camera Shutter

A responsive and 100% JavaScript-free camera shutter component library entry. The interlocking mechanical blades smoothly retract and rotate outward to open the aperture when hovered.

## Features
- **Pure CSS:** Transitions and `clip-path` animations with zero JS overhead.
- **Smooth Mechanics:** 6 geometric blade layers built with hardware-accelerated transforms.
