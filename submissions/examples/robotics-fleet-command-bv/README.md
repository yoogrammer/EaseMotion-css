# Robotics Fleet Command Center

A premium enterprise robotics dashboard with fleet activity tracking, battery status gauges, and navigation log streams using pure CSS.

## Usage
Open `demo.html` directly in a browser — no server needed.

## Why EaseMotion CSS?
Demonstrates premium CSS layout, pulse animations, hover transitions, and fade-up effects with zero JS dependencies.