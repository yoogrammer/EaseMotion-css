const demo = document.querySelector('.demo-wrapper');

// No JS needed - clamp() works natively
// This file exists to match the template pattern
console.log('Fluid typography uses CSS clamp() - resize the browser!');
