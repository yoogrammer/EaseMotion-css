# Animated Fluid Grid

A responsive CSS grid gallery of eight gradient-colored boxes with staggered fade-in animation, hover scale, and border glow. The grid uses `auto-fill` with `minmax` to reflow smoothly on any screen size. Open `demo.html` in a browser for an instant, no-code visual gallery component.
