# Responsive Tooltip Fallback

1. What does this do? Flips top-oriented tooltips to bottom positions on mobile layouts to prevent clipping.
2. How is it used? Apply tooltip elements using pseudo-elements which reposition under mobile media queries.
3. Why is it useful? Improves mobile layout readability and prevents elements from flowing out of the viewport.
