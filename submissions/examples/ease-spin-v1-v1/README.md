# Ease Spin Animation

A pure CSS continuous 360° rotation utility framework feature that supports custom speeds and directional configurations.

## Features
- Continuous seamless rotation loops using pure CSS `@keyframes`.
- Adjustable tracking with the custom `--ease-spin-speed` CSS variable.
- Built-in `ease-spin-reverse` element wrapper for uniform counter-rotations.
