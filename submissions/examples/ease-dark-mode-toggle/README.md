# Pure CSS Dark Mode Toggle Component

An efficient, lightweight theme implementation demonstrating interface state toggles cleanly inside the core rendering tree.

## Technical Method
- Utilizes the checkbox-hack state toggle design paradigm to control variables entirely without script dependency loops.
- Employs general sibling selectors (`~`) to interpolate design tokens on targeted layouts.