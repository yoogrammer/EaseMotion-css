# CSS Confetti Rain Burst Component

A hardware-accelerated celebration module that runs particle rendering pipelines completely inside a single CSS keyframes execution engine.

## Layout Attributes
- Offloads animation processing down to custom color properties to balance CPU load cycles.
- Integrates systemic motion tracking overrides to safely protect user visibility profiles when system accessibility parameters are active.