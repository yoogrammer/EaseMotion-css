# Navbar Contrast Fallback

1. What does this do? Ensures text remains legible in dark mode on browsers that lack backdrop blur support.
2. How is it used? Uses supports queries to apply solid dark fallback background colors.
3. Why is it useful? Prevents white-on-white text rendering on older browser engines.
