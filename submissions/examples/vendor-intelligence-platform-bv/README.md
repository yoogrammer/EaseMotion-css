# Vendor Intelligence Platform

A premium vendor management dashboard with third-party API performance metrics, SLA status tables, and cost analysis using pure CSS.

## Usage
Open `demo.html` directly in a browser — no server needed.

## Why EaseMotion CSS?
Demonstrates premium CSS layout, pulse animations, hover transitions, and fade-up effects with zero JS dependencies.
