# Scroll Margin Utilities

**What does this do?**  
Demonstrates `scroll-margin` with classes like `scroll-mt-1`, `scroll-mt-2`, `scroll-mb-1`.

**Why?**  
Adds offset when scrolling to elements, useful for fixed/sticky header clearance.
