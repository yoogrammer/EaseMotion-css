# Buffer Loading Pulse

1. What does this do? Shows a media buffering indicator with staggered pulsing bars.
2. How is it used? Apply `.buffer-loader` to a container with `.buffer-bar` children.
3. Why is it useful? It gives video, audio, and content loading states a clear CSS-only waiting cue.
