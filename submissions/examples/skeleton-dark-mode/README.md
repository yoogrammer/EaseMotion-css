# Skeleton Dark Mode

Adds dark mode color support for the `ease-skeleton-shimmer` utility.

## What it fixes

The skeleton shimmer used light neutral colors in dark mode, which caused poor contrast on dark backgrounds.

## Usage

```html
<div class="ease-skeleton-shimmer skeleton-line"></div>