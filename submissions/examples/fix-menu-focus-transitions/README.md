# Fix ease-menu Focus Transitions

## What does this do?
Standardizes the focus ring and adds smooth transitions for all interactive states (hover, active, focus) for the `menu` component.

## How is it used?
Include the component as usual. The new CSS handles all state transitions automatically when interacting via mouse or keyboard.

## Why is it useful?
Improves user experience by providing fluid visual feedback and ensures consistent accessibility indicators.
Fixes: #37147
