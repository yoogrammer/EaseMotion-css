# Scale Stagger Children

Children of a parent container scale in sequentially from zero with staggered delays.

## Features
- Scale-from-zero entrance for each child
- Staggered `transition-delay` via `--i`
- `--animate` custom property toggle

## Control
Click "Toggle Animate" to scale children in/out with stagger.
