# Quantum Research Operations Hub

A premium enterprise quantum computing dashboard with qubit stability logs, cryogenic temperature charts, and experiment telemetry using pure CSS.

## Usage
Open `demo.html` directly in a browser — no server needed.

## Why EaseMotion CSS?
Demonstrates premium CSS layout, quantum glow animations, hover transitions, and fade-up effects with zero JS dependencies.