# Ease Image Zoom Effect

### What does this do?
Zooms the image smoothly inside its cropped container boundaries on hover.

### How is it used?
Put an image with `.ease-image-zoom-pr` inside a container with `overflow: hidden`.

### Why is it useful?
Standard elegant UI effect for modern blog cards, e-commerce listings, and galleries.