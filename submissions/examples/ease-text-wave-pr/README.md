# Ease Text Wave Animation

### What does this do?
Animates text characters sequentially to create a wave ripple loading effect.

### How is it used?
Split text characters into spans with `--i` Custom Properties matching their indexes under a `.ease-text-wave-pr` header.

### Why is it useful?
Excellent for eye-catching loading pages or splash headers without relying on heavy JS libraries.