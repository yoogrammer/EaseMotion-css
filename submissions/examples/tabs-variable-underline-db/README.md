# Variable Tabs Underline

1. What does this do? Sets tab underline widths and translations dynamically using custom count values.
2. How is it used? Pass --tab-count and --active-idx style variables to parent container elements.
3. Why is it useful? Removes duplicate hardcoded selectors for tabs layout configurations.
