# CSS will-change Performance Example (#9155)

## Description
This example demonstrates the proper use of the `will-change` property to optimize animations by promoting elements to their own compositing layers.

## Why EaseMotion CSS?
As a motion-first library, performance is key. Using `will-change` prevents layout jank and ensures smooth 60fps animations.