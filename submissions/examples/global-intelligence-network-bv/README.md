# Global Intelligence Network

A premium enterprise dashboard with worldwide node logs, incident tables, and data routing performance stats using pure CSS.

## Usage
Open `demo.html` directly in a browser — no server needed.

## Why EaseMotion CSS?
Demonstrates premium CSS layout, scan animations, hover transitions, and fade-up effects with zero JS dependencies.