# Enterprise Audit Intelligence Hub

A premium enterprise audit dashboard with financial audit trails, ledger sync logs, and active transaction checking using pure CSS.

## Usage
Open `demo.html` directly in a browser — no server needed.

## Why EaseMotion CSS?
Demonstrates premium CSS layout, pulse animations, hover transitions, and fade-up effects with zero JS dependencies.