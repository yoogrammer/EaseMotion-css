# Star Rating Component

## Summary
Pure CSS 5-star rating component using radio inputs and sibling selectors. Zero JavaScript.

## How it works
`flex-direction: row-reverse` + CSS `~` sibling selector highlights hovered and checked stars.