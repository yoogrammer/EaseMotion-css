# css-variable-scope-fix

Standalone fix for [Issue 9220](https://github.com/SAPTARSHI-coder/EaseMotion-css/issues/9220). Defines custom properties inside the component selector rather than :root, preventing global leakage during animations.
