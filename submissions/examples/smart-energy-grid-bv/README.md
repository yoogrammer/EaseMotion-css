# Smart Energy Grid Dashboard

A premium enterprise energy dashboard with grid consumption trackers, peak load metrics, and secondary substation status logs using pure CSS.

## Usage
Open `demo.html` directly in a browser — no server needed.

## Why EaseMotion CSS?
Demonstrates premium CSS layout, pulse animations, hover transitions, and fade-up effects with zero JS dependencies.