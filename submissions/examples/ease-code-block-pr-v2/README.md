# Syntax-Highlighted Code Block Component

A reusable documentation card component featuring custom monospace gutter line indexing, a styled layout panel, and integrated clipboard copy logic.

## Layout Features
- Native layout structure built with semantic elements.
- Uses distinct token selection rules to apply specific styling constraints to keywords, assignments, and structural components.