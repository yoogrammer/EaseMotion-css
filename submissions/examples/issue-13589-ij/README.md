# CTA Banner

An animated call-to-action banner with a moving gradient background, pulsing button, stats counters, and scroll-triggered entrance.

## Usage

Include `style.css` in your project and structure your CTA with the provided `.cta-banner` class.

## Demo

Open `demo.html` in a browser to see the component in action.
