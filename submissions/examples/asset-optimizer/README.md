# Contributor Sandbox Asset Optimizer 🖼️

A standalone Node.js utility helper to losslessly compress showcase `.png`, `.jpg`, and `.gif` files right in your local sandbox before staging changes.

## Setup Instructions

Navigate to the utility directory and pull down dependencies:
```bash
cd submissions/examples/asset-optimizer
npm install
```