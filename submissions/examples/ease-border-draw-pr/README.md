# Ease Border Drawing Animation

### What does this do?
Triggers a stylized outline draw effect that trace borders around a card sequentially on hover.

### How is it used?
Create a container with `.ease-border-draw-pr` containing four empty `span` elements and the core content.

### Why is it useful?
Provides an elegant highlight outline visual effect for dashboard modules and modern buttons.