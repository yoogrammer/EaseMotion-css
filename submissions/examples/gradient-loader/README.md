# Rotating Gradient Loader

A CSS-only circular loader that uses a rotating gradient effect to indicate loading states.

## Features

- Pure CSS
- Infinite rotation animation
- Reduced-motion accessibility support
- No JavaScript required

## Usage

```html
<div class="gradient-loader" aria-label="Loading"></div>
```

The loader uses a conic gradient combined with rotation animation to create a smooth loading indicator.