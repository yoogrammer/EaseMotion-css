# Global Risk Observatory

A premium enterprise risk dashboard with risk rating indicators, compliance trackers, and worldwide active alerts using pure CSS.

## Usage
Open `demo.html` directly in a browser — no server needed.

## Why EaseMotion CSS?
Demonstrates premium CSS layout, blink animations, hover transitions, and fade-up effects with zero JS dependencies.