# Future Mobility Operations Center

A premium enterprise mobility dashboard with urban transit telemetry charts, battery storage stats, and dispatcher logs using pure CSS.

## Usage
Open `demo.html` directly in a browser — no server needed.

## Why EaseMotion CSS?
Demonstrates premium CSS layout, pulse animations, hover transitions, and fade-up effects with zero JS dependencies.