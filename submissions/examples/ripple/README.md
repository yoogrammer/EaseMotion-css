# ease-ripple

A pure CSS Material Design-style click ripple effect using `:active::after` pseudo-element. No JavaScript required.

---

## What it does

Creates a smooth ripple animation that spreads outward when a user clicks an element. Built entirely with CSS using pseudo-elements and keyframes.

---

## Usage

```html
<button class="ease-ripple">
  Click me
</button>