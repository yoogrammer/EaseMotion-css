# Animated Auth Card

A dark-themed login card with a rotating gradient glow border, floating label inputs, and a hover-glowing submit button. Open `demo.html` in any browser to use it — no server or build step needed. This provides an eye-catching authentication UI that works entirely with pure CSS, making it easy to drop into any project as a styled form component.
