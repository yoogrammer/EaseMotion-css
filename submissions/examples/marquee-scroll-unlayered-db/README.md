# Global Marquee Keyframes

1. What does this do? Declaratively lifts scroll keyframes outside cascade layers to fix browser rendering bugs.
2. How is it used? Uses unlayered root keys to drive the continuous track translations.
3. Why is it useful? Resolves animation lookup failures on Safari and older Chromium browsers.
