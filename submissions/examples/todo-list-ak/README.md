# Todo List

## What
A todo list with add, toggle completion, delete, and filter (all/active/completed) functionality with localStorage persistence.

## How
Uses a JavaScript array of todo objects stored in localStorage. Checkbox toggles the `done` status, filters show subsets, and the count displays remaining active items.

## Why
Demonstrates a classic CRUD todo application with client-side persistence and filtering, essential for task management.
